@{
    var user = User.Identity.Name;
    
}
<div id="layoutSidenav_nav">
    <nav class="sb-sidenav accordion sb-sidenav-dark" id="sidenavAccordion">
        <div class="sb-sidenav-menu">
            <div class="nav">
                <div class="sb-sidenav-menu-heading">Account</div>
                @if (User.Identity.IsAuthenticated)
                {
                    <a class="nav-link" asp-action="Logout" asp-controller="Account">
                        <div class="sb-nav-link-icon"><i class="fas fa-sign-out-alt"></i></div>
                        logout
                    </a>
                  
                    @*<a class="nav-link" asp-area="Admin" asp-action="Index" asp-controller="Admin">
                        <div class="sb-nav-link-icon"><i class="fas fa-user-cog"></i></div>
                        Admin
                    </a>*@
                    <a class="nav-link" asp-area="UserPanel" asp-action="Index" asp-controller="UserPanel">
                        <div class="sb-nav-link-icon"><i class="fas fa-user"></i></div>
                        User Panel
                    </a>
                }
                else
                {

                    <a class="nav-link" asp-action="Login" asp-controller="Account">
                        <div class="sb-nav-link-icon"><i class="fas fa-sign-in-alt"></i></div>
                        Login
                    </a>
                    <a class="nav-link" asp-action="CheckReferralForRegister" asp-controller="Account">
                        <div class="sb-nav-link-icon"><i class="fas fa-registered"></i></div>
                        Register
                    </a>
                }
               


            </div>
        </div>
        @if (User.Identity.IsAuthenticated)
        {
            <div class="sb-sidenav-footer">
                <div class="small">Logged in as:</div>
                @User.Identity.Name
            </div>
        }
        else
        {
    <div class="sb-sidenav-footer">
        <div class="small">Logged in as:</div>
        guest
    </div>
        }
    </nav>
</div>
